<!DOCTYPE html>
<html ng-app="notesApp"><html>
  <% include partials/nav.ejs %>  
  <body ng-controller="notesCtrl">
  <% include partials/sidebar.ejs %>  
    <div class="container-fluid" ng-init="getHeadlines()">
      <div class="row">
        <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
          
          <h1 class="page-header">Dashboard - Video Headlines</h1>
          
          <div class="col-sm-12">
            Video Headlines and Summaries below.
            {{imgSize}}
          </div>
          <br><br>
          <form class="col-md-12 form-group input-group-lg">
            <input ng-model="query" type="text" class="form-control" placeholder="Search..." autofocus>
          </form>

          <div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>
                    <a href="#" ng-click="sortType = 'follow.articleData.title'; sortReverse = !sortReverse">
                      Title
                      <span ng-show="sortType == 'follow.articleData.title' && !sortReverse" class="fa fa-caret-up"></span>
                      <span ng-show="sortType == 'follow.articleData.title' && sortReverse" class="fa fa-caret-down"></span>
                    </a>
                  </th>
                  <th>
                    Summary
                  </th>
                  <th>
                    Count
                  </th>
                  <th>
                    Image
                  </th>
                  <th>Delete Headline</th>
                </tr>
              </thead>
              <tbody>
                <tr ng-repeat="follow in headlines | filter:query | unique: '_id' | orderBy:sortType:sortReverse">
                  <td><a href="{{follow.url}}" target="_blank">{{ follow.articleData.title }}</a></td>
                  <td>{{ follow.articleData.summary }}</td>
                  <td>{{ follow.count }}</td>
                  <td><a href="https://cdn.tutsplus.com/net/uploads/legacy/958_placeholders/placehold.gif" target="_blank"><img src="https://cdn.tutsplus.com/net/uploads/legacy/958_placeholders/placehold.gif" height="42" width="42"></a>
                  <button class="btn btn-link" type="submit" ng-click="create(follow.articleData.summary, follow._id)">Create Image</button>
                  </td>
                  <td>
                    <button class="btn btn-link" type="submit" ng-click="removeHeadline(follow._id)"><i class="fa fa-times"></i></button>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>

   </body>
   <% include partials/javascripts.ejs %>  
</html>